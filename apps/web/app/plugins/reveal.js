let io = null;
function watcher() { if (io) return io; io = new IntersectionObserver(entries => { for (const e of entries) { if (!e.isIntersecting) continue; e.target.dataset.in = ""; io.unobserve(e.target); } }, { rootMargin: "0px 0px -10% 0px", threshold: 0.06 }); return io; }
export default defineNuxtPlugin(nuxt => { const dead = import.meta.client && !("IntersectionObserver" in window); if (dead) document.documentElement.classList.remove("reveal-on"); nuxt.vueApp.directive("reveal", { getSSRProps(binding) { const props = { "data-reveal": binding.arg || "" }; if (binding.value != null) props.style = { "--i": String(binding.value) }; return props; }, mounted(el, binding) { if (dead) return; if (binding.value != null) el.style.setProperty("--i", String(binding.value)); el.dataset.reveal = binding.arg || ""; watcher().observe(el); }, unmounted(el) { io?.unobserve(el); } }); });
